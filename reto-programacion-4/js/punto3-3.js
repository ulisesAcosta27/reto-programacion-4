const tipoDeSuscripcion = 'Basic';

if(tipoDeSuscripcion != 'Free' && tipoDeSuscripcion == 'Basic' && tipoDeSuscripcion != 'Exert' && tipoDeSuscripcion != 'ExpertPlus'){
    console.log("Puedes tomar casi todos los cursos durante un mes");
}